import { StyleSheet } from "react-native";

export const contactStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
  view: {
    marginVertical: "30%",
    width: "100%",
    display: "flex",
    paddingHorizontal: 40,
  }
});
