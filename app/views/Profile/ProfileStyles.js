import { StyleSheet } from "react-native";

export const profileStyles = StyleSheet.create({
  view: {
    marginVertical: 30,
    paddingHorizontal: 30,
  },
  logoutButton: {
    marginTop: 60,
  },
});
