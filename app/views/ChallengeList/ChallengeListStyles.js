import { StyleSheet } from "react-native";

export const challengeListStyles = StyleSheet.create({
  container: {
    padding: 20,
  },
  item: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    padding: 10,
    marginVertical: 8,
  },
});
